<!DOCTYPE html>
<html>
  <head>
    <title>Fly</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <title>Bss Bss</title>
  </head>

  <body>
    <nav class="navbar navbar-default">
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <a class="navbar-brand" href="<%= root_path %>">Travel wif fly</a>
          <ul class="nav navbar-nav navbar-right">
            <% if !current_user%>
                <li><a href="<%= new_user_session_path %>" >log in</a></li>
                <li><a href="<%= new_user_registration_path %>" >Sing up</a></li>
            <% else %>
            <li><a href="<%= destroy_user_session_path %>" data-method="delete" >Sing up</a></li>
            <% end %>
          </ul>
          </nav>
    <%= yield %>
  </body>
</html>
